<h1>New criterion</h1>

<% form_for([@subject, @criterion]) do |f| %>
  <%= f.error_messages %>

<%= render :partial => 'form', :object=>f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', criterions_path %>