<h1>New Indicator</h1>
<%= "New Indicator for \"#{@standard.name}\" under the subject \"#{@standard.name}\"" %>

<% form_for([@standard, @indicator]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :label %><br />
    <%= f.text_field :label %>
  </p>
  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </p>

    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', @standard %>