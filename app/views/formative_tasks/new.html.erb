<h1>New formative_task</h1>

<% form_for([@unit_planner, @formative_task]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :object=>f%>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', formative_tasks_path %>